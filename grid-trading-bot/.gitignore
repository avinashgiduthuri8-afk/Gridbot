.env
__pycache__/
*.pyc
.pytest_cache/
.pytest_tmp/
dashboard/static/
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
data/.drive_snapshot_*
logs/*.log
logs/*.log.*
.venv/
venv/

# Google Drive service account key (if kept inside the project instead of
# an external path) — never commit credentials.
*service-account*.json
*serviceaccount*.json
gdrive-*.json
